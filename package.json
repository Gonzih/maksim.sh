{
  "name": "maksim-sh",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "knowledge:generate": "node scripts/generate-knowledge.mjs",
    "knowledge:validate": "node scripts/validate-knowledge.mjs",
    "build": "npm run knowledge:generate && npm run knowledge:validate && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
